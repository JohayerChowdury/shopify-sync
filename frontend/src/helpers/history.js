import { createBrowserHistory, createMemoryHistory } from 'history';

const browserHistory = createBrowserHistory();
const memoryHistory = createMemoryHistory();

export { browserHistory, memoryHistory };
