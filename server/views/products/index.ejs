<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"
      integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T"
      crossorigin="anonymous"
    />
    <title>All products</title>
  </head>
  <body>
    <div class="container">
      <h1 class="mb-4"><%=store.name%>'s Products</h1>
      <form
        action="/shopify_api/stores/<%= store.storeId %>/products"
        method="POST"
        class="d-inline"
      >
        <button type="submit" class="btn btn-success">
          Sync Store Products from Shopify
        </button>
      </form>
      <a href="../<%=store.storeId%>" class="btn btn-secondary">Back</a>
      <% products.forEach(product => { %>
      <div class="card mt-4">
        <div class="card-body">
          <h4 class="card-title"><%= product.title %></h4>
          <div class="card-text mb-2">
            Shopify ID: <%= product.product_id %>
          </div>
          <div class="card-subtitle text-muted mb-2">
            <%=product.created_at.toLocaleDateString()%>
          </div>
          <a
            href="/shopify_api/stores/<%= store.storeId %>/products/<%=product.product_id%>"
            class="btn btn-primary"
            >See More</a
          >
        </div>
      </div>
      <% }) %>
    </div>
  </body>
</html>
